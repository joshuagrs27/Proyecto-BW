@model IEnumerable<Proyecto.Models.Platillo>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nombre)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.sabor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.precio)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idRestaurante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.idTurista)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nombre)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.sabor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.precio)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idRestaurante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.idTurista)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idPlatillo }) |
            @Html.ActionLink("Details", "Details", new { id=item.idPlatillo }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idPlatillo })
        </td>
    </tr>
}

</table>
